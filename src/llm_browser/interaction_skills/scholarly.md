# Scholarly Search

Use `load_skill("scholarly")` for paper, biomedical, encyclopedia, or citation-heavy queries.

```python
load_skill("scholarly")
search_scholarly("Hafnia alvei animals")
```

This is an API fallback skill. Use normal web search first for commercial, product, company, or people queries.
